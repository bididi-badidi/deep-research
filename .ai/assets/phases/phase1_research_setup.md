# Phase 1: Research & Setup

## Goals
Establish the project foundation using modern Python tools (`uv`) and define the initial structure and configuration.

## Tasks
- [x] Initialize project with `uv`
- [x] Set up `pyproject.toml`
- [x] Create directory structure (`src/`, `.ai/assets/`)
- [x] Create `.env` template
